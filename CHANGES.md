# 추가_제출 변경 이력

## 2026-05-08 변경사항

### 1. 로그아웃 DOM 팝업 처리 (`automation/tasks.py`)
로그아웃 버튼 클릭 후 "로그아웃 하시겠습니까?" DOM 팝업이 뜨는 경우 `handle_dom_popup`으로 확인 버튼 자동 클릭.

### 2. 사람 간 1초 대기 (`main.py`)
한 사람의 모든 처리가 끝난 후 다음 사람 진행 전 `time.sleep(1)` 추가.  
process_row가 실제 실행된 경우(성공/실패 모두)에만 적용, 주민번호 형식 오류 등 사전 skip은 해당 없음.

### 3. 첨부 파일명 검증 (`automation/tasks.py`)
부속서류 제출하기 버튼 클릭 **전**에 그리드 첫 번째 행 파일명 셀을 읽어 PDF 파일명과 일치 여부 확인.

- 확인 셀: `#mf_pf_UTECMGAA06_grdAddDocTery_cell_0_3 > nobr` (innerText)
- 불일치 시: 팝업 닫기 → `(False, "파일명_불일치")` 반환

### 4. 폴더별 실패 기록 (`main.py`)
실패 발생 시 `input/pdf/<폴더명>/실패.txt` 파일 생성.  
파일 내용: 이름 / 실패 사유 / 기록 시각.  
기존 `failed_log.txt`는 유지(전체 요약용).

---

## 실패가 기록되는 케이스 목록

| 실패 사유 | 발생 조건 |
|---|---|
| `주민번호_형식_이상` | 엑셀에서 읽은 주민번호가 13자리 숫자 형식이 아닌 경우 |
| `pdf파일_없음` | `input/pdf/<폴더명>/<파일명>` 경로에 파일이 존재하지 않는 경우 |
| `pdf파일이_아님` | 파일은 있으나 확장자가 `.pdf`가 아닌 경우 |
| `페이지_로드_실패` | URL 진입 후 주민번호 입력 필드가 15초 내 나타나지 않는 경우 |
| `주민번호_입력_실패` | 주민번호 입력 후 JS 값 검증이 일치하지 않는 경우 |
| `조회된_데이터_없음` | 조회 팝업에 "조회된 데이터가 없습니다" 포함 — 해당 연도 신고내역 자체가 없는 경우 |
| `첨부하기_창_열기_실패` | 첨부하기 버튼 클릭 후 새 창이 열리지 않거나 파일선택 버튼이 25초 내 미감지 |
| `파일_업로드_실패` | 파일선택 후 그리드 `apndFleNm` 컬럼에 파일명이 10초 내 반영되지 않는 경우 |
| `파일명_불일치` | 그리드에 올라온 파일명이 제출하려는 PDF 파일명과 다른 경우 |
| `제출버튼_클릭_실패` | 부속서류 제출하기 버튼을 10초 내 찾지 못하거나 클릭 예외 발생 |
| `예외_<ExceptionType>` | process_row 내부에서 예상치 못한 Python 예외 발생 |

> **참고:** `주민번호_형식_이상`, `pdf파일_없음`, `pdf파일이_아님`은 브라우저 조작 없이 사전 검증 단계에서 발생하며, 이 경우 `navigate` 플래그가 `True`로 리셋되고 폴더 실패 기록도 남음.
